<Window x:Class="prakticke_zadani.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:prakticke_zadani"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinHeight="300" MinWidth="700">
    <Grid>
        <Label Content="Auta" HorizontalAlignment="Center" Margin="0,24,0,0" VerticalAlignment="Top" Width="310" HorizontalContentAlignment="Center" FontSize="30"/>
        <Label Content="Celková cena vozů prodaných o víkendu :" HorizontalAlignment="Left" Margin="80,94,0,0" VerticalAlignment="Top"/>
        <Label Content="Všechna vozidla :" HorizontalAlignment="Left" Margin="400,94,0,0" VerticalAlignment="Top"/>

        <DataGrid x:Name="CarSumTable"
          Margin="80,125,480,75"
          AutoGenerateColumns="False"
          IsReadOnly="True">

            <DataGrid.Columns>
                <DataGridTemplateColumn>
                    <DataGridTemplateColumn.Header>
                        <StackPanel>
                            <TextBlock Text="Název modelu"/>
                            <TextBlock Text="Cena bez DPH"/>
                        </StackPanel>
                    </DataGridTemplateColumn.Header>

                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Model}"/>
                                <TextBlock Text="{Binding PriceWithoutDph, StringFormat={}{0:N2} Kč}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Cena s DPH" Binding="{Binding PriceWithDph, StringFormat={}{0:N2} Kč}"/>
            </DataGrid.Columns>
        </DataGrid>

        <DataGrid x:Name="CarTable"
          Margin= "400,125,80,75"
          AutoGenerateColumns="False"
          IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Model"
                            Binding="{Binding Model}"/>

                <DataGridTextColumn Header="Datum"
                            Binding="{Binding Date}"/>

                <DataGridTextColumn Header="Cena"
                            Binding="{Binding Price}"/>

                <DataGridTextColumn Header="DPH"
                            Binding="{Binding Dph}"/>

                <DataGridTemplateColumn Header="">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Name="Delete" Content="Smazat"
                            Click="DeleteCar_btn_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Margin="20,10,20,10" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Name="Unload_btn" Content="Smazat vše" Margin="5" Click="Unload_btn_Click"/>
            <Button Grid.Column="1" Name="Load_btn" Content="Načíst soubor" Margin="5" Click="Load_btn_Click"/>
            <Button Grid.Column="2" Name="Export_btn" Content="Exportovat" Margin="5" Click="Export_btn_Click"/>
            <Button Grid.Column="3" Name="AddCar_btn" Content="Přidat auto" Margin="5" Click="AddCar_btn_Click"/>
        </Grid>
    </Grid>
</Window>
